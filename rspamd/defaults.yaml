# -*- coding: utf-8 -*-
# vim: ft=yaml
rspamd:
  use_upstream_repo: true
  repo:
    humanname: Rspamd Official Repository

  pkg: rspamd

  base_dir: /etc/rspamd

  config:
    local: {}
    override: {}

  service:
    name: rspamd

