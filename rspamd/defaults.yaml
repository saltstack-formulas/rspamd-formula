# -*- coding: utf-8 -*-
# vim: ft=yaml
---
rspamd:
  use_upstream_repo: true
  repo:
    humanname: Rspamd Official Repository

  manage_redis: false
  redis_pkg: redis
  redis_service: redis

  pkg: rspamd

  base_dir: /etc/rspamd

  config:
    local: {}
    override: {}

  service:
    name: rspamd
